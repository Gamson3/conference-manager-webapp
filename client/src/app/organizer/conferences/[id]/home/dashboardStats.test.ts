import { describe, it } from "vitest";

describe.skip("organizer home dashboard stats (deprecated)", () => {
	it("is covered by the server dashboard stats endpoint", () => {
		// Intentionally skipped.
	});
});
